# Local SQLite database (relative path — recommended for local dev)
DATABASE_URL="file:./db/custom.db"

# For production (Vercel / managed Postgres), use:
# DATABASE_URL="postgresql://user:password@host:port/dbname?schema=public"
